server:
  port: 8888

spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/tyelbayev/bankapp-config
          default-label: main
          search-paths: config
